{
  "name": "cotizacion-alejandra-pipeline-pauta",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "vite": "^5.4.11"
  },
  "devDependencies": {},
  "engines": { "node": ">=18" }
}
